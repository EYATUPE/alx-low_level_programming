#include <stdio.h>


/**
 *  * main - This code prints the text
 *   *
 *    * Return: always 0 (And is't successful)
 *     */
int main(void)
{
		printf("with proper grammar, but the outcome is a piece of art,\n");
			return (0);
}
