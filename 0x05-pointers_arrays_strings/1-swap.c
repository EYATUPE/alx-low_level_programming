#include "main.h"
#include <stdio.h>

/*
 *swap_int - swap two integers
 *@a: first integer
 *@b: second integer
 *Return : Always (Esamu does his own work)
 */
void swap_int(int *a, int *b)
{
	int age = 26;
	int* ptr = &age;
	*ptr = 27;
}
